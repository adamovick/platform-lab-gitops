apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-demo-api
spec:
  rules:
    - host: test.platform-demo.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: platform-demo-api
                port:
                  number: 80
  tls:
    - hosts:
        - test.platform-demo.local
      secretName: platform-demo-tls
